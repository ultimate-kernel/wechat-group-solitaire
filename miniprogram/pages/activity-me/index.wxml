<view class="container">
  <view class="activity-list">
    <mp-cells>
      <mp-cell title="活动" wx:for="{{activityList}}" wx:key="index" wx:for-index="idx" link data-item="{{item}}" bindtap="goActivityDetail">
        <text>{{item.activityTitle || '---'}}</text>
      </mp-cell>
    </mp-cells>
  </view>
</view>
